todo sphere:

fix bug with score if not logged in (new?)			X
api more info (title year)							X
string constants for translations					X
add first/prev/next/last in api. Buttons?			?
green/red overlay instead of dialog.ok 				?
hotkeys for random/guess/next/prev/first/last		?
only use returns									X
hover für guess?
move cookie to a xbmc-legal path!					X
escapte return title_year
add "checking..." to title_year						X
rename title_year
make random after guess_right optional. def:enabled